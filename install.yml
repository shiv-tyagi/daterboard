apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-daterboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-daterboard
  template:
    metadata:
      labels:
        app: web-daterboard
    spec:
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 1
              preference:
                matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - microboo
      containers:
      - name: nginx
        image: nginx:1.27.3-alpine
        ports:
        - containerPort: 80
        volumeMounts:
        - name: html-content
          mountPath: /usr/share/nginx/html
        resources:
          requests:
            cpu: "50m"
            memory: "50Mi"
          limits:
            cpu: "50m"
            memory: "50Mi"
      initContainers:
      - name: git-clone
        image: alpine/git:v2.47.1
        args:
          - clone
          - --depth=1
          - https://github.com/shiv-tyagi/daterboard.git
          - /tmp/html
        volumeMounts:
        - name: html-content
          mountPath: /tmp/html
      volumes:
      - name: html-content
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: web-daterboard-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: web-daterboard
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: web-daterboard
spec:
  parentRefs:
    - name: eg
  hostnames:
    - "daterboard.bigliboo.dev"
  rules:
    - backendRefs:
        - group: ""
          kind: Service
          namespace: default
          name: web-daterboard-service
          port: 80
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /
